/**
 * Re-export of the single Alert component for backward compatibility.
 * Use Alert from @/components/Alert or showAppAlert() for new code.
 */
export {
  Alert as InfoModal,
  type AlertProps as InfoModalProps,
  type AlertVariant as InfoModalVariant,
} from '@/components/Alert';
